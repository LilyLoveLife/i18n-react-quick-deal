describe("replace-group1", () => {
    it("first unit test", () => {
        expect(1 + 1).toBe(2);
    });
});

describe("replace-group2", () => {
    it("first unit test", () => {
        expect(1 + 1).toBe(2);
    });
});